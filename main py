import os
import requests
from flask import Flask

app = Flask(__name__)

# Telegram Credentials
TELEGRAM_BOT_TOKEN = "8767606359:AAH7dZn_9dsT1HwmOkbvKAB2bgB2aEvOz0c"
TELEGRAM_CHAT_ID = "6449682719"

@app.route("/")
def home():
    return "Bot is Live!"

def send_test_message():
    url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage"
    payload = {
        "chat_id": TELEGRAM_CHAT_ID,
        "text": "🚀 Hello! Aapka Forex Bot Render par sahi se chal gaya hai!"
    }
    try:
        res = requests.post(url, json=payload)
        print("Telegram Response:", res.json())
    except Exception as e:
        print("Error:", e)

# App start hote hi ek baar message bhejega
send_test_message()

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 10000))
    app.run(host="0.0.0.0", port=port)
